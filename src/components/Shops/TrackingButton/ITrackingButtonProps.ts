export interface ITrackingButtonProps {
  /**
   * The Url for Tracking service
   * @type {String}
   *
   * */
  trackingUrl: string;

  /**
   * Disables the button
   * @type {boolean}
   *
   * */
  disabled?: boolean;
}
