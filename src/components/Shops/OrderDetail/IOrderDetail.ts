export interface IOrderDetail {
  /**
   * The items's name
   * @type {String}
   *
   * */
  name: string;

  /**
   * The items's quantity bought
   * @type {String}
   *
   * */
  qty: number;

  /**
   * The items's price
   * @type {string}
   *
   * */
  price: string;

  /**
   * The items's image
   * @type {String}
   *
   * */
  image: string;

  /**
   * The items's image format
   * @type {String}
   *
   * */
  imageFormat: string;
}
