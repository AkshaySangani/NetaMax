export interface ICardLayoutProps {
  /**
   * The text displayed on the purple banner above the card
   * @type string
   */
  bannerText: string;

  /**
   * Array with string to define the gradient colors
   * @type string[]
   */
  gradientColors?: string[];
}
