export interface ICoupon {
  /**
   * The coupon's id number.
   * @type {string}
   */
  id: string;

  /**
   * The coupon name.
   * @type {string}
   */
  name: string;

  /**
   * The disccount percentaje number.
   * @type {string}
   */
  discountPercentage: string;

  /**
   * The disccount amount number.
   * @type {string}
   */
  discountAmount: string;
}
