import React, { ReactElement } from "react";

import { ShopsLoginContainer } from "containers/Shops/ShopsLogin/ShopsLoginContainer";

/**
 * The page with the shops login
 * @returns {ReactElement} The shops's login page.
 */
const ShopsLogin = (): ReactElement => {
  return <ShopsLoginContainer />;
};

export default ShopsLogin;
